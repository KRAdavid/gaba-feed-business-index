name: Legacy public index refresh (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement workflow
        run: |
          echo "This legacy workflow is intentionally disabled for push and schedule events."
          echo "Use 'Auto-update GABA feed intelligence v2' for data refreshes."
          echo "Use 'Deploy GABA public index v2' for GitHub Pages deployment."
          echo "Use 'Install guided product ordering' for the product order guide."
