# GABA Feed 문의메일 직접수신 설정

## 현재 오류의 의미

페이지에 `메일 서버 응답이 지연되고 있습니다`가 표시되면 프런트엔드 문의폼은 실행됐지만, Google Apps Script 웹 앱의 `doPost(e)`가 현재 배포본에서 응답하지 않았다는 뜻입니다. 가장 흔한 원인은 다음 세 가지입니다.

1. `Inquiry_v2.gs`가 Apps Script 프로젝트에 아직 설치되지 않음
2. 파일은 저장했지만 기존 웹 앱을 **새 버전으로 재배포하지 않음**
3. 스프레드시트·이메일 발송 권한을 아직 승인하지 않음

## 최초 1회 설치 및 복구

1. `GABA_Feed_Intelligence_Master_DB`를 엽니다.
2. 상단에서 **확장 프로그램 → Apps Script**를 선택합니다.
3. 왼쪽 파일 목록에서 기존 `Inquiry` 파일을 열거나 새 스크립트 파일 `Inquiry`를 만듭니다.
4. 기존 내용을 모두 지우고 저장소의 `apps-script/Inquiry_v2.gs` 전체 내용을 붙여넣습니다.
5. 저장합니다.
6. 상단 함수 선택에서 `setupGabaInquiryReceiver`를 선택하고 **실행**합니다.
7. 권한 요청에서 스프레드시트 편집과 이메일 발송을 승인합니다.
8. 실행 결과로 Master DB에 `Inquiries` 시트가 생기고 다음 두 주소로 테스트 메일이 와야 합니다.
   - `dubaissday@cellpinda.com`
   - `dubaissday@gmail.com`
9. 우측 상단 **배포 → 배포 관리**를 엽니다.
10. 기존 웹 앱의 연필 아이콘을 누릅니다.
11. 버전에서 **새 버전**을 선택하고 배포합니다.
12. 설정은 **실행 사용자: 나**, **액세스 권한: 모든 사용자**를 유지합니다.

기존 배포를 새 버전으로 갱신하면 현재 `/exec` URL은 유지됩니다.

## 이번 v2 수정사항

- FormSubmit 중계서비스를 완전히 제거했습니다.
- Apps Script가 문의를 직접 이메일로 발송합니다.
- `HtmlService.XFrameOptionsMode.ALLOWALL`을 적용해 공개 페이지의 숨김 응답 프레임이 접수 결과를 받을 수 있게 했습니다.
- 응답 대기시간을 60초로 늘렸습니다.
- 문의자 이메일을 Reply-To로 사용합니다.
- 모든 답변과 메일 발송 상태를 `Inquiries` 시트에 기록합니다.
- 문의번호 `INQ-YYYYMMDD-HHMMSS-XXXXXX`를 발급합니다.

## 정상 작동 확인

1. 공개 페이지를 `Ctrl + F5`로 새로고침합니다.
2. 테스트 문의를 전송합니다.
3. 페이지에 `문의가 정상 접수되었습니다`와 문의번호가 표시되는지 확인합니다.
4. 두 수신함과 스팸함을 확인합니다.
5. `Inquiries` 시트에 한 행이 추가됐는지 확인합니다.

## 결과별 진단

- `Inquiries` 시트 자체가 없음: 설치 함수가 실행되지 않았거나 권한 승인이 안 된 상태
- 시트는 있으나 새 행이 없음: 웹 앱이 새 버전으로 배포되지 않았거나 외부 접근권한이 제한된 상태
- 새 행은 있으나 `Mail_Status=failed`: `Mail_Error`와 Apps Script 실행 로그 확인
- 메일은 도착했으나 페이지가 시간초과: `Inquiry_v2.gs`의 ALLOWALL 버전이 배포되지 않은 상태
- 60초 후 시간초과: 먼저 시트와 메일을 확인한 뒤 기록이 없을 때만 재전송하여 중복 문의를 방지

## 개인정보 운영

문의정보는 사업협업 대응 목적으로만 사용합니다. 주민등록번호, 계좌번호, 건강정보, 비밀번호 또는 영업비밀 원문을 수집하지 않습니다.
